@using Microsoft.AspNetCore.Http
@inject IHttpContextAccessor HttpContextAccessor
@using Microsoft.AspNetCore.Identity
@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager
@inject IAdminService AdminService
@* @functions {
    public bool IsUserAdmin()
    {
        return User.Identity?.IsAuthenticated == true && User.IsInRole("Admin");
    }

    public bool IsUserAuthenticated()
    {
        return User.Identity?.IsAuthenticated == true;
    }
} *@

@{
    var currentStudentId = HttpContextAccessor.HttpContext?.Session.GetString("CurrentStudentId");
    var isSignedIn = SignInManager.IsSignedIn(User);
    var user = await UserManager.GetUserAsync(User);
    var userRoles = user != null ? await UserManager.GetRolesAsync(user) : new List<string>();
    var isAdmin = userRoles.Contains("SuperAdmin") || userRoles.Contains("UniversityAdmin") ||
                  userRoles.Contains("FacultyAdmin") || userRoles.Contains("DepartmentAdmin") ||
                  userRoles.Contains("FinanceAdmin") || userRoles.Contains("StudentAdmin") ||
                  userRoles.Contains("Admin");
    var isSuperAdmin = userRoles.Contains("SuperAdmin");

    // Get pending applications count safely
    int pendingCount = 0;
    if (isSuperAdmin)
    {
        try
        {
            var applications = await AdminService.GetPendingApplicationsAsync();
            pendingCount = applications?.Count ?? 0;
        }
        catch
        {
            // If service fails, show 0
            pendingCount = 0;
        }
    }
}

<div class="position-sticky pt-3">
    <!-- User Profile Section -->
    <div class="text-center text-white mb-4 p-3 bg-primary bg-opacity-25 rounded">
        <div class="mb-3">
            <i class="fas fa-university fa-2x text-warning"></i>
        </div>
        <h6 class="mb-1">Academic Portal</h6>
        <small>Welcome to Student Management System</small>
        @if (isSignedIn)
        {
            <div class="mt-2">
                @if (isSuperAdmin)
                {
                    <span class="badge bg-danger">
                        <i class="fas fa-crown me-1"></i>Super Admin
                    </span>
                }
                else if (userRoles.Contains("UniversityAdmin"))
                {
                    <span class="badge bg-primary">
                        <i class="fas fa-university me-1"></i>University Admin
                    </span>
                }
                else if (userRoles.Contains("FacultyAdmin"))
                {
                    <span class="badge bg-info">
                        <i class="fas fa-chalkboard-teacher me-1"></i>Faculty Admin
                    </span>
                }
                else if (userRoles.Contains("DepartmentAdmin"))
                {
                    <span class="badge bg-success">
                        <i class="fas fa-building me-1"></i>Department Admin
                    </span>
                }
                else if (userRoles.Contains("FinanceAdmin"))
                {
                    <span class="badge bg-warning">
                        <i class="fas fa-money-bill me-1"></i>Finance Admin
                    </span>
                }
                else if (userRoles.Contains("StudentAdmin"))
                {
                    <span class="badge bg-secondary">
                        <i class="fas fa-user-graduate me-1"></i>Student Admin
                    </span>
                }
                else if (userRoles.Contains("Admin"))
                {
                    <span class="badge bg-success">
                        <i class="fas fa-user-shield me-1"></i>Administrator
                    </span>
                }
                else if (userRoles.Contains("Faculty"))
                {
                    <span class="badge bg-info">
                        <i class="fas fa-chalkboard-teacher me-1"></i>Faculty
                    </span>
                }
                else if (userRoles.Contains("Student"))
                {
                    <span class="badge bg-primary">
                        <i class="fas fa-user-graduate me-1"></i>Student
                    </span>
                }
            </div>
        }
        else if (!string.IsNullOrEmpty(currentStudentId))
        {
            <div class="mt-2">
                <span class="badge bg-success">
                    <i class="fas fa-user me-1"></i>Student Mode
                </span>
            </div>
        }
    </div>

    <!-- Navigation Menu -->
    <ul class="nav flex-column">
        <!-- Dashboard Section -->
        <li class="nav-item">
            <div class="section-header">DASHBOARD</div>
        </li>
        <li class="nav-item">
            <a class="nav-link @IsActive("Home", "Index")" asp-controller="Home" asp-action="Index">
                <i class="fas fa-home me-2"></i>Home
            </a>
        </li>

        <!-- Student Portal Section -->
        @if (!string.IsNullOrEmpty(currentStudentId) || userRoles.Contains("Student"))
        {
            <li class="nav-item">
                <div class="section-header">STUDENT PORTAL</div>
            </li>

            @if (!string.IsNullOrEmpty(currentStudentId))
            {
                <li class="nav-item">
                    <a class="nav-link @IsActive("Registration", "StudentDashboard")"
                       asp-controller="Registration" asp-action="StudentDashboard"
                       asp-route-studentId="@currentStudentId">
                        <i class="fas fa-tachometer-alt me-2"></i>Student Dashboard
                    </a>
                </li>
            }

            <li class="nav-item">
                <a class="nav-link @IsActive("Registration", "StudentPortalEntry")"
                   asp-controller="Registration" asp-action="StudentPortalEntry">
                    <i class="fas fa-sign-in-alt me-2"></i>Student Login
                </a>
            </li>
        }

        <!-- Admin System Section -->
        @if (isAdmin)
        {
            <li class="nav-item">
                <div class="section-header">ADMIN SYSTEM</div>
            </li>

            @if (isSuperAdmin)
            {
                <li class="nav-item">
                    <a class="nav-link @IsActive("AdminManagement", "Index")"
                       asp-controller="AdminManagement" asp-action="Index">
                        <i class="fas fa-user-shield me-2"></i>Admin Management
                    </a>
                </li>

                <li class="nav-item">                    
                    <a class="nav-link @IsActive("Index", "Applications")"
                       asp-controller="Applications" asp-action="Index">
                        <i class="fas fa-clipboard-list me-2"></i>Admin Applications
                    </a>
                    <a class="nav-link @IsActive("AdminManagement", "Applications")"
                       asp-controller="AdminManagement" asp-action="Applications">
                        <i class="fas fa-clipboard-list me-2"></i>Admin Pending Applications
                        @if (pendingCount > 0)
                        {
                            <span class="badge bg-danger ms-2">@pendingCount</span>
                        }
                    </a>
                </li>

                <li class="nav-item">
                    <a class="nav-link @IsActive("AdminManagement", "Dashboard")"
                       asp-controller="AdminManagement" asp-action="Dashboard">
                        <i class="fas fa-tachometer-alt me-2"></i>Admin Dashboard
                    </a>
                </li>

                <li class="nav-item">
                    <a class="nav-link @IsActive("UserManagement", "Index")"
                       asp-controller="UserManagement" asp-action="Index">
                        <i class="fas fa-users-cog me-2"></i>User Management
                    </a>
                </li>
                <li>
                    <a class="dropdown-item" href="@Url.Action("EmailAnnouncements", "AdminManagement")">
                        <i class="fas fa-bullhorn me-2"></i>Email Announcements
                    </a>
                </li>
                <!-- Add to System dropdown or create new menu -->
                <li>
                    <a class="dropdown-item" href="@Url.Action("EmailSettings", "AdminManagement")">
                        <i class="fas fa-envelope me-2"></i>Email Settings
                    </a>
                </li>
                <!-- In your navbar or homepage -->
                @* @if (!IsUserAuthenticated() || !IsUserAdmin())
                {
                    <li class="nav-item">
                        <a class="nav-link" href="@Url.Action("Apply", "Applications")">
                            <i class="fas fa-user-plus me-2"></i>Apply for Admin Access
                        </a>
                    </li>
                } 
                @if (SignInManager.IsSignedIn(User))
                {
                    <li class="nav-item">
                        <a class="nav-link" href="@Url.Action("Apply", "Applications")">
                            <i class="fas fa-user-plus me-2"></i>Apply for Admin Access
                        </a>
                    </li>
                }*@
            }

            <!-- Academic Management Section -->
            <li class="nav-item">
                <div class="section-header">ACADEMIC MANAGEMENT</div>
            </li>

            <!-- University Structure -->
            <li class="nav-item">
                <a class="nav-link @IsActive("University", "Index")" asp-controller="University" asp-action="Index">
                    <i class="fas fa-building me-2"></i>Universities
                </a>
            </li>

            <li class="nav-item">
                <a class="nav-link @IsActive("Colleges", "Index")" asp-controller="Colleges" asp-action="Index">
                    <i class="fas fa-school me-2"></i>Colleges
                </a>
            </li>

            <li class="nav-item">
                <a class="nav-link @IsActive("Departments", "Index")" asp-controller="Departments" asp-action="Index">
                    <i class="fas fa-layer-group me-2"></i>Departments
                </a>
            </li>

            <li class="nav-item">
                <a class="nav-link @IsActive("Branches", "Index")" asp-controller="Branches" asp-action="Index">
                    <i class="fas fa-code-branch me-2"></i>Branches
                </a>
            </li>

            <!-- Core Academic Management -->
            <li class="nav-item">
                <a class="nav-link @IsActive("Semesters", "Index")" asp-controller="Semesters" asp-action="Index">
                    <i class="fas fa-calendar-alt me-2"></i>Semester Management
                </a>
            </li>

            <li class="nav-item">
                <a class="nav-link @IsActive("Students", "Index")" asp-controller="Students" asp-action="Index">
                    <i class="fas fa-users me-2"></i>Student Management
                </a>
            </li>

            <li class="nav-item">
                <a class="nav-link @IsActive("Courses", "Index")" asp-controller="Courses" asp-action="Index">
                    <i class="fas fa-book me-2"></i>Course Management
                </a>
            </li>

            <!-- Grading System -->
            <li class="nav-item">
                <div class="section-header">GRADING SYSTEM</div>
            </li>

            <li class="nav-item">
                <a class="nav-link @IsActive("Grading", "Dashboard")" asp-controller="Grading" asp-action="Dashboard">
                    <i class="fas fa-chart-bar me-2"></i>Grading Dashboard
                </a>
            </li>

            <li class="nav-item">
                <a class="nav-link @IsActive("Grading", "ManageComponents")" asp-controller="Grading" asp-action="ManageComponents">
                    <i class="fas fa-cogs me-2"></i>Grading Components
                </a>
            </li>

            <li class="nav-item">
                <a class="nav-link @IsActive("Grading", "EnterGrades")" asp-controller="Grading" asp-action="EnterGrades">
                    <i class="fas fa-edit me-2"></i>Enter Grades
                </a>
            </li>

            <li class="nav-item">
                <a class="nav-link @IsActive("Grading", "FinalGrades")" asp-controller="Grading" asp-action="FinalGrades">
                    <i class="fas fa-list-alt me-2"></i>Final Grades
                </a>
            </li>

            <li class="nav-item">
                <a class="nav-link @IsActive("Grading", "Reports")" asp-controller="Grading" asp-action="Reports">
                    <i class="fas fa-chart-pie me-2"></i>Grade Reports
                </a>
            </li>

            <!-- Registration System Dropdown -->
            <li class="nav-item">
                <div class="section-header">REGISTRATION SYSTEM</div>
            </li>

            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="registrationDropdown" role="button"
                   data-bs-toggle="dropdown" aria-expanded="false">
                    <i class="fas fa-clipboard-list me-2"></i>Registration System
                </a>
                <ul class="dropdown-menu" aria-labelledby="registrationDropdown">
                    <li><h6 class="dropdown-header">Core Management</h6></li>
                    <li>
                        <a class="dropdown-item" asp-controller="Registration" asp-action="Management">
                            <i class="fas fa-cogs me-2 text-primary"></i>Registration Management
                        </a>
                    </li>
                    <li>
                        <a class="dropdown-item" asp-controller="Registration" asp-action="Rules">
                            <i class="fas fa-ruler-combined me-2 text-warning"></i>Registration Rules
                        </a>
                    </li>
                    <li>
                        <a class="dropdown-item" asp-controller="Registration" asp-action="Periods">
                            <i class="fas fa-calendar-alt me-2 text-info"></i>Registration Periods
                        </a>
                    </li>

                    <li><hr class="dropdown-divider"></li>

                    <li><h6 class="dropdown-header">Analytics & Reports</h6></li>
                    <li>
                        <a class="dropdown-item" asp-controller="Registration" asp-action="Analytics">
                            <i class="fas fa-chart-bar me-2 text-info"></i>Registration Analytics
                        </a>
                    </li>
                    <li>
                        <a class="dropdown-item" asp-controller="Registration" asp-action="Reports">
                            <i class="fas fa-chart-pie me-2 text-success"></i>Registration Reports
                        </a>
                    </li>

                    <li><hr class="dropdown-divider"></li>

                    <li><h6 class="dropdown-header">Student Management</h6></li>
                    <li>
                        <a class="dropdown-item" asp-controller="Registration" asp-action="StudentHistorySelection">
                            <i class="fas fa-history me-2 text-secondary"></i>Registration History
                        </a>
                    </li>

                    <li><hr class="dropdown-divider"></li>

                    <li><h6 class="dropdown-header">Quick Actions</h6></li>
                    <li>
                        <a class="dropdown-item" asp-controller="Registration" asp-action="RegistrationDashboard">
                            <i class="fas fa-tachometer-alt me-2 text-primary"></i>Registration Dashboard
                        </a>
                    </li>

                    <li><hr class="dropdown-divider"></li>

                    <li><h6 class="dropdown-header">Student Access</h6></li>
                    <li>
                        <a class="dropdown-item" asp-controller="Registration" asp-action="StudentPortalEntry">
                            <i class="fas fa-graduation-cap me-2 text-success"></i>Student Portal Login
                        </a>
                    </li>
                </ul>
            </li>

            <!-- Resources & Tools Section -->
            <li class="nav-item">
                <div class="section-header">RESOURCES & TOOLS</div>
            </li>

            <li class="nav-item">
                <a class="nav-link @IsActive("QRCode", "Index")" asp-controller="QRCode" asp-action="Index">
                    <i class="fas fa-qrcode me-2"></i>QR Code Attendance
                </a>
            </li>

            <li class="nav-item">
                <a class="nav-link @IsActive("CourseMaterials", "Index")" asp-controller="CourseMaterials" asp-action="Index">
                    <i class="fas fa-file-alt me-2"></i>Course Materials
                </a>
            </li>

            <li class="nav-item">
                <a class="nav-link" asp-controller="Home" asp-action="AcademicCalendar">
                    <i class="fas fa-calendar me-2"></i>Academic Calendar
                </a>
            </li>

            <li class="nav-item">
                <a class="nav-link" asp-controller="Home" asp-action="Reports">
                    <i class="fas fa-chart-line me-2"></i>System Reports
                </a>
            </li>
        }
    </ul>

    <!-- Quick Stats -->
    <div class="mt-4 p-3 bg-dark bg-opacity-50 rounded text-white">
        <h6 class="border-bottom pb-2">System Status</h6>
        <div class="small">
            <div class="d-flex justify-content-between mb-2">
                <span>Active Students:</span>
                <span class="badge bg-success">@(ViewBag.TotalStudents ?? "0")</span>
            </div>
            <div class="d-flex justify-content-between mb-2">
                <span>Active Courses:</span>
                <span class="badge bg-info">@(ViewBag.ActiveCourses ?? "0")</span>
            </div>
            <div class="d-flex justify-content-between">
                <span>Current Semester:</span>
                <span class="badge bg-warning">@(ViewBag.CurrentSemesterName ?? "N/A")</span>
            </div>
            @if (isSuperAdmin && pendingCount > 0)
            {
                <div class="d-flex justify-content-between mt-2 pt-2 border-top">
                    <span>Pending Apps:</span>
                    <span class="badge bg-danger">@pendingCount</span>
                </div>
            }
        </div>
    </div>
</div>

@functions {
    public string IsActive(string controller, string action)
    {
        var currentController = ViewContext.RouteData.Values["Controller"]?.ToString();
        var currentAction = ViewContext.RouteData.Values["Action"]?.ToString();

        return (currentController == controller && currentAction == action) ? "active" : "";
    }
}